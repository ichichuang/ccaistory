# 清理验收报告

## 1. 执行概要

| 项目 | 值 |
|---|---|
| 清理执行时间 | 2026-06-24 17:43 CST |
| 仓库 | ichichuang/ccaistory |
| 当前分支 | `clean/purge-story-assets` |
| 起始分支 | `main`（清理前工作区干净，无未提交改动） |
| 任务类型 | 仅清理（不重构、不迁移、不生成新故事/图片、不建立新架构） |
| 结果 | 通过：所有成果资产/试运行产物/运行缓存已清除；底层系统完整保留 |

---

## 2. 删除目录清单

以下目录已从工作树完整删除：

| 目录 | 删除原因 | 删除前内容 |
|---|---|---|
| `故事项目/` | 具体故事项目容器（必删清单） | `.gitkeep`、`故事项目总览.md`（含 `KHN2_001` 引用） |
| `资产库/` | 成果资产容器（必删清单） | `资产库总览.md` |
| `输出归档/` | 输出归档容器（必删清单） | `输出归档总览.md` |
| `原始资料/` | 故事输入素材容器；不在保留白名单内（判断删除，见 §10） | `资料来源清单.md`（当前素材数=0） |
| `runtime/.runs/` | runtime 运行缓存 / 历史 run（必删清单） | `.gitkeep` + 试运行 run 产物（run_manifest、checkpoint） |
| `runtime/.artifacts/` | artifact registry 运行数据（必删清单） | `.gitkeep`、`registry.json`（artifacts 为空 `{}`） |

同时删除的个人状态文件：

| 文件 | 删除原因 |
|---|---|
| `.obsidian/workspace.json` | Obsidian 工作区/最近打开文件状态，残留 `KHN2_001_楼道里的红贴纸/...`、`R00_人工外部生成操作单`、`Compiled_Prompts`、`Semantic_Lint_Result`、`Source_Pilot_Task_List` 等旧项目路径 |
| `.obsidian/app.json` | Obsidian app 状态（内容为空 `{}`，按 §二要求删除 app state） |

`.obsidian/appearance.json`（`{}`）与 `.obsidian/core-plugins.json`（通用插件开关）无任何项目路径，按要求保留 `.obsidian/` 而保留之。

清理过程中一并清除的可再生缓存（未跟踪，已从磁盘移除）：
`runtime/.runs/`（试运行重新生成的 run）、`__pycache__/`（17 个）、根目录 `.DS_Store`。

---

## 3. 删除文件类型清单

- 故事项目总览 / 资产库总览 / 输出归档总览 / 资料来源清单（空状态占位 Markdown）
- runtime artifact registry 运行数据（`registry.json`）
- runtime 运行缓存 / run manifest / checkpoint（试运行产物）
- Obsidian 个人工作区与 app 状态（含旧项目路径引用）
- Python 字节码缓存（`__pycache__`）、系统 `.DS_Store`

> 说明：仓库中**未发现**任何真实成果资产实体文件（compiled prompt 实例、semantic lint result 实例、source pilot 任务清单、image execution package、候选图/接受图/拒绝图、cover/scene image、publishing package、telemetry run report 等）。这些产物此前均存放于已删除的 `故事项目/KHN2_001_...` 项目目录中，当前工作树已不含该目录（仅在 `.obsidian/workspace.json` 的最近文件列表中残留路径字符串，已随该文件删除）。

---

## 4. 保留目录清单（底层系统）

| 目录 | 性质 |
|---|---|
| `.git/` | 版本库 |
| `.obsidian/`（仅 appearance.json、core-plugins.json） | 通用编辑器配置 |
| `.gitignore` | 新建（见 §7） |
| `总控/` | 总控编排（已清理污染） |
| `Codex指令/` | Codex 运行指令 |
| `工作流/` | 工作流定义 |
| `生产技能/` | 生产技能 |
| `提示词库/` | 提示词库 |
| `结构规范/` | 结构规范（已清理污染） |
| `验收清单/` | 验收规则 |
| `模板/` | 模板 |
| `创作者技法库/` | 创作者技法库 |
| `runtime/` | 工具层代码、contracts、schemas、tests（见 §5） |

> 注：`Codex指令/*.md` 中形如 `故事项目/<project_id>/故事核心.json` 的路径是**通用 I/O 路径约定**（占位符 `<project_id>`），属底层工作流文档，予以保留；`故事项目/` 空容器本身已删除，新项目按该约定在运行时创建。

---

## 5. runtime 保留模块清单

工具层代码、contracts、tests 全部保留，逻辑未改动：

| 模块 | 状态 |
|---|---|
| `runtime/contracts/` | 保留 |
| `runtime/skill_registry/` | 保留 |
| `runtime/pipeline_runner/` | 保留 |
| `runtime/story_analyzer/` | 保留 |
| `runtime/skill_executor/` | 保留 |
| `runtime/prompt_compiler/` | 保留 |
| `runtime/lint_engine/` | 保留 |
| `runtime/artifact_registry/` | 保留 |
| `runtime/multimodal_qa/` | 保留 |
| `runtime/tests/` | 保留 |

附加工具层模块（均为代码，非产物，保留）：
`runtime/core/`、`runtime/graph_engine/`、`runtime/qa_engine/`、`runtime/schemas/`、`runtime/skill_orchestrator/`、`runtime/skill_runtime/`、`runtime/state_machine/`、`runtime/telemetry/`、`runtime/aistory.py`、`runtime/README.md`。

### runtime 唯一改动（测试代码，非工具逻辑）

`runtime/tests/test_runtime_smoke.py`：第 660 行 `story_project_root.iterdir()` 此前无存在性守卫，在 `故事项目/` 被删除后会抛 `FileNotFoundError`。已加最小守卫 `if story_project_root.exists():`，与第 58 行快照守卫一致。**断言语义不变**（目录缺失 ⇒ 无越权产物），仅防止规范命令 `smoke-test` 崩溃。属 §六允许的测试代码调整，未触碰任何工具逻辑。

### tests/fixtures 判定

`runtime/tests/fixtures/*.json` 经核查为**通用 mock 数据**（`project_id=fixture_project`、`asset_id=fixture_asset_r00`、`story_id=fixture_story`，asset_type 使用引擎分类枚举），不含 `KHN2_001`、`楼道里的红贴纸`、真实故事正文或真实图像资产，故全部保留，测试可继续通过。

---

## 6. 关键词搜索结果

### 6.1 真实项目标识（必须为 0，结果均为 0）

| 关键词 | 命中文件数 |
|---|---|
| `KHN2_001` | 0 |
| `楼道里的红贴纸` | 0 |
| `红贴纸` | 0 |
| `R00_人工外部生成操作单` | 0 |
| `R00_PAPER`（独立 asset id，非 `_MARK_ANCHOR`） | 0 |
| `CLEAN_WHITE_NOTEBOOK` | 0 |
| `manual_external_generation` / `manual_external_generation_r00` | 0 |
| `source_pilot_ready`（作为故事产物）/ `WebGPT试产` / `六资产试产` / `出图执行包` | 0（产物层） |

清理动作：
- `总控/常用指令.md`、`总控/总索引.md`：`下一个项目编号：KHN2_001` → `PROJECT_ID_EXAMPLE`。
- `总控/总索引.md`、`总控/正式维护文件清单.md`：移除指向已删除空状态总览的失效 wiki 链接（故事项目/资产库/输出归档/原始资料）。
- `结构规范/视觉资产本体结构规范.md`：style_policy 中 `克制红贴纸点缀` → `克制红色标记点缀`（去除 `红贴纸` 字样，保留通用红色点缀风格语义）。

### 6.2 引擎词汇（属底层系统，按设计保留并说明）

§九列出的 `compiled_prompt`、`semantic_lint`、`artifact_registry`、`source_pilot_ready` 同时是 runtime **引擎的固有词汇**（模块名、artifact 类型名、schema 字段、流水线状态名、CLI 用法示例），遍布 `结构规范/`、`提示词库/`、`工作流/`、`生产技能/`、`Codex指令/`、`验收清单/` 以及 runtime 代码/schemas/contracts。它们是“底层工作流/技能/工具代码”本身，删除即等于摧毁本次要求保留的系统，故按设计**保留**：

| 引擎词汇 | 命中文件数 | 性质 |
|---|---|---|
| `compiled_prompt` | 77 | prompt 编译器 artifact 类型 / schema / 文档 |
| `semantic_lint` | 65 | 语义 Lint 引擎模块 / 类型 / 文档（含 .gitignore 规则 1 项） |
| `artifact_registry` | 15 | Artifact Registry 工具模块 |
| `source_pilot_ready` | 2 | 状态机状态名（`validate_contracts.py`、`state_machine.json`） |
| `R00_PAPER_MARK_ANCHOR` | 27 | 引擎视觉资产**类型枚举**（非 `R00_PAPER` 项目 asset id） |

> 关键澄清：本次禁删的真实标识是 `R00_PAPER`（红贴纸项目的具体 asset id），经全仓核查**独立形式 0 命中**。代码/合同/schema/fixtures 中出现的是 `R00_PAPER_MARK_ANCHOR`，系引擎抽象资产类型，贯穿 `if asset_type == "R00_PAPER_MARK_ANCHOR"` 等控制流，删改即违反“不得修改 runtime 工具代码逻辑”并使全部测试失败，故保留。`R00/R01/R02/P01/S` 同理为资产类型角色码，非故事实例。

### 6.3 残留 `KHN2` 说明

唯一残留 `KHN2` 位于 `runtime/contracts/validate_contracts.py:166`：
```python
HISTORICAL_STORY_PATTERNS = ["KHN2_", "故事000", "story_project_"]
```
这是引擎**反污染守卫**逻辑（用于拒绝历史故事命名），与 `KHN2_001` 项目实例无关，属工具代码逻辑，保留。`KHN2_001` 全仓 0 命中。

---

## 7. .gitignore 更新内容

仓库此前**无 `.gitignore`**，本次新建，包含 §八全部要求规则并补充 Python/缓存项：

```gitignore
# runtime 运行产物 / 缓存
runtime/.runs/  runtime/.artifacts/  runtime/artifacts/
runtime/output/  runtime/outputs/  runtime/cache/
runtime/tmp/  runtime/temp/  runtime/logs/  runtime/reports/  runtime/generated/
runtime/compiled/  runtime/source_pilot/  runtime/semantic_lint_results/  runtime/qa_results/

# 未来架构工作目录
01-raw/  50-agent-work/  90-archive/

# 图像与二进制资产（canonical 资产卡用 .md 提交）
*.png *.jpg *.jpeg *.webp *.gif *.psd *.ai *.zip *.mp4 *.mov

# Python 缓存
__pycache__/  *.py[cod]  .pytest_cache/

# 系统与 Obsidian 个人状态
.DS_Store  .obsidian/workspace*  .obsidian/cache/  .obsidian/app.json  .obsidian/graph.json
```

> 说明：按 §八，原始图像/候选图/生成图/失败图默认不提交；canonical reference asset 的 Markdown 卡（`.md`）不受影响，可正常提交。

---

## 8. 二进制资产搜索结果

全仓递归扫描 `.png/.jpg/.jpeg/.webp/.gif/.psd/.ai/.pdf/.zip/.mp4/.mov`：

**命中 0 个。** 仓库当前不含任何图像、压缩包、视频或设计源文件。无需人工确认项。

---

## 9. 验收门禁结果

| 门禁 | 结果 |
|---|---|
| 是否仍有成果资产残留 | 否 |
| 是否仍有旧故事项目残留（KHN2_001 / 楼道里的红贴纸） | 否 |
| 是否仍有 compiled prompt / semantic lint / source pilot / R00 操作单等旧生产产物实例 | 否 |
| 是否仍有 `runtime/.runs` / `runtime/.artifacts` | 否（目录已删，已加 .gitignore） |
| 是否仍有候选图 / 失败图 / accepted 图 / 发布图像 | 否（全仓 0 二进制资产） |
| 底层 12 项目录是否完整保留 | 是 |
| runtime 10 模块是否完整保留 | 是 |
| runtime 测试是否通过 | 是 |
| 是否生成了非许可的新文件 | 否（仅本报告 + `.gitignore`，均为许可项） |

### 测试结果（`python runtime/aistory.py ...`）

> 环境中 `pytest` 因缺失 `pygments` 依赖无法导入；但项目测试入口为 `runtime/aistory.py smoke-test`（`test_runtime_smoke.py` 暴露 `run_smoke_tests()`，无 pytest 可收集用例），与 pytest 无关。

| 命令 | 结果 |
|---|---|
| `smoke-test` | `status: pass`，failed_checks = 0 |
| `validate` | `status: pass` |
| `validate-contracts` | `status: pass`（skill_count = 12） |
| `check-contract-drift` | `status: pass`（passed: true） |

---

## 10. 判断与边界说明

- **删除 `原始资料/`**：该目录不在 §二保留白名单内（白名单为穷举式“只允许保留”），且属故事输入素材容器（与 `故事项目/资产库/输出归档` 同类、概念对应未来 `01-raw/`），当前素材数为 0。据白名单规则与清理意图判断删除，并在此显式记录。
- **保留 `R00_PAPER_MARK_ANCHOR` 及 `R00/R01/R02/P01/S` 类型码、`compiled_prompt/semantic_lint/artifact_registry/source_pilot_ready` 引擎词汇**：均为底层引擎/合同/schema/状态机词汇，删除将违反“不得修改 runtime 工具代码逻辑”并破坏全部测试与底层系统。§九“关键词不存在”按其语义理解为**故事产物实例层面**为 0（已满足），引擎词汇层面按设计保留并在 §6.2 全量列明。
- **保留 `validate_contracts.py` 的 `KHN2_` 守卫**：反污染逻辑，非项目实例。
- **`runtime/tests/fixtures/` 全保留**：经核查为通用 mock，不含真实故事/图像。
- **无“需人工确认”二进制项**：全仓 0 二进制资产。

---

## 11. 下一阶段就绪结论

✅ **可进入下一阶段 Obsidian Story Production Wiki 重构。**

当前仓库只保留底层工作流、技能、结构规范、模板、验收规则、Codex 指令、runtime 工具代码与 contracts；不含任何具体故事项目、成果资产、试运行产物、图像候选、run cache 或 artifact registry 数据；runtime 全部门禁通过。底层系统干净、自洽、可执行，具备进入新架构重构的前置条件。

> 本次未建立 `01-raw/ 02-wiki/ 50-agent-work/ 90-archive/` 等新架构目录，未迁移旧故事，未生成任何新故事/图片/Prompt/执行包，严格符合“仅清理”边界。
