---
type: skill_card
id: "资产验收与返修技能"
title_zh: 资产验收与返修技能
title_en: Asset Acceptance & Repair Router Skill
status: active
project_id: ""
related_assets: []
source_paths: []
tags: [skill, story-lab]
created_at: 2026-06-25
updated_at: 2026-06-25
owner: ichichuang
version: v1
canonical: true
skill_category: repair
runtime_commands: []
runtime_support_status: manual_only
input_layer: "50-agent-work"
output_layer: "50-agent-work, 90-archive"
human_gate: yes
qa_gate: no
workflow_dependencies: []
replacement_for: ""
deprecated_by: ""
---
# 资产验收与返修技能

> 事实源声明（canonical doctrine）：本卡 canonical 知识存于 `02-wiki` Markdown；`runtime/contracts` 仅定义校验规则；执行期 JSON 属派生 runtime/agent 产物。下文凡提及“写入 story_core / 故事核心.json / 派生视图”，一律按本卡 frontmatter 的 `input_layer`/`output_layer` 落地：操作记录写入 `50-agent-work`，持久决策回填 `02-wiki` canonical 卡，原始输入留在 `01-raw`，被拒材料入 `90-archive`。runtime 与 Artifact Registry 仅为派生缓存。

## 1. 技能目的
把 rejected/rework 资产路由到正确上游并保护 accepted 依赖。

## 2. 输入
- 故事核心.json
- 上游已通过门禁的派生视图
- 对应结构规范、模板、提示词锁和验收清单

## 3. 输出
- repair_queue、accepted_assets、依赖解锁记录
- machine_state 更新建议
- blocked_actions 或返修入口

## 4. 读取的唯一事实源字段
- asset_acceptance、repair_policy、reference_dependencies

## 5. 允许创建的派生视图
- repair_queue、accepted_assets、依赖解锁记录
- 派生视图必须登记到 `story_core.derived_views` 或 `story_core.visual_pipeline`
- 派生视图不得成为第二事实源

## 6. 禁止行为
- 不跳过状态机和上游门禁。
- 不创建故事外图片、执行包、发布包、备份或复盘归档。
- 不调用 WebGPT 或外部出图工具，除非技能职责明确为人工外部执行后的事实回填。
- 不使用作者姓名作为风格指令，不引用历史故事名、历史 asset_id 或旧项目。
- 不把 rejected/rework 资产当作 accepted 使用。

## 7. 门禁
- 输入字段存在且与当前 machine_state 匹配。
- 输出字段能回写到 story_core.visual_pipeline.asset_results、repair_queue。
- 对视觉链路：没有 compiled_prompt 不得出图；没有 execution_telemetry 和 actual_prompt_sent_to_external_tool 不得 accepted。
- 对 R00：不得出现人物、火柴人、完整场景、道具集合或符号散点表；R00 未 accepted 不得继续 R01/R02。

## 8. 失败回退位置
- 字段缺失：回退到故事核心状态机技能。
- 上游门禁失败：回退到 资产级细粒度验收技能。
- 本技能输出不合格：回退到本技能重新生成或最近上游技能。
- 外部执行事实缺失：回退到图像执行遥测技能；无法补录则 blocked。

## 9. 上游依赖
- 资产级细粒度验收技能

## 10. 下游入口
- 平台发布页合成技能或源插图Prompt编译技能

## 11. 必须写入 story_core 的字段
- visual_pipeline.asset_results、visual_pipeline.repair_queue

## 12. 必须检查的验收清单
- 资产级细粒度验收清单
