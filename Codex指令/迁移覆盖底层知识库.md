# 迁移覆盖底层知识库

任务：将本包迁移覆盖到 AI+Story 项目根目录。

## 前置
- 工作目录必须为 `/Users/cc/MyPorject/AI+Story`。
- 用户必须提供解压后的包路径。
- 不删除 `.git`。
- 不运行 Git 命令。
- 不创建故事，不生成图片，不生成执行包，不生成发布包。

## 操作
1. 清空并重建：总控、生产技能、提示词库、结构规范、工作流、验收清单、模板、Codex指令、创作者技法库。
2. 清空并重建空状态目录：故事项目、原始资料、资产库、输出归档。
3. 从包路径复制对应目录。
4. 运行只读巡检。

## 返回
是否完成、覆盖目录、故事数量、图片数量、发布包数量、正式入口、视觉执行编译层完整性、断链结果。
