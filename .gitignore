# ── runtime 运行产物 / 缓存（不提交） ─────────────────────────
runtime/.runs/
runtime/.artifacts/
runtime/artifacts/
runtime/output/
runtime/outputs/
runtime/cache/
runtime/tmp/
runtime/temp/
runtime/logs/
runtime/reports/
runtime/generated/
runtime/compiled/
runtime/source_pilot/
runtime/semantic_lint_results/
runtime/qa_results/

# ── 工作层：01-raw / 50-agent-work / 90-archive ──────────────
# 默认忽略这三层的内容（原始素材、生成产物、归档资产不入库），
# 但用 allowlist 保留目录骨架：README.md 与 .gitkeep 仍然跟踪。
# canonical 知识层 02-wiki 与系统层 00-system 不在此列，完整跟踪。
01-raw/**
!01-raw/**/
!01-raw/**/README.md
!01-raw/**/.gitkeep

50-agent-work/**
!50-agent-work/**/
!50-agent-work/**/README.md
!50-agent-work/**/.gitkeep

90-archive/**
!90-archive/**/
!90-archive/**/README.md
!90-archive/**/.gitkeep

# ── 图像与二进制资产（默认不提交；canonical 资产卡用 .md / .base 提交） ──
# 覆盖原始图、生成图、候选图、accepted/rejected 图、视频、压缩包、设计源文件。
*.png
*.jpg
*.jpeg
*.webp
*.gif
*.psd
*.ai
*.zip
*.mp4
*.mov

# ── Python 缓存 ──────────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/

# ── 系统与 Obsidian 个人状态 ─────────────────────────────────
.DS_Store
.obsidian/workspace*
.obsidian/cache/
.obsidian/app.json
.obsidian/graph.json
# 社区插件代码为第三方下载产物（不提交，避免仓库塞入压缩 JS）；
# 已启用插件清单由 community-plugins.json 跟踪，可在 Obsidian 内一键重装。
.obsidian/plugins/
